const sum = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const devide = (a, b) => a / b;

const calculator = {
    sum,
    subtract,
    multiply,
    devide
};
module.exports = calculator;